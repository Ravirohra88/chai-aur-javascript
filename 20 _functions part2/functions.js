function calculatecart(value1,value2,...num1) {//rest operator
    return num1
}
console.log(calculatecart(200,333,666,788));

const customer = {
    name:" ravi",
    bill: 1000,
}
function customerHandaling(anyobject) {
    console.log(`customer name is ${anyobject.name} and bill is ${anyobject.bill}`);
}
// customerHandaling(customer)

customerHandaling({
    name:"rohit",
    bill:10000
})

const myarray = [1,2,3,4,5,6,7,8,9]

function printingArray(arrayfetch) 
{
    return arrayfetch
    
}
console.log(printingArray(myarray));
